{
  "name": "@hunch/mobile",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "react-native start",
    "android": "react-native run-android",
    "build": "echo 'mobile build handled by metro'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@hunch/tsconfig": "workspace:*",
    "typescript": "^5.7.0"
  }
}
