EXPO_PUBLIC_API_URL=http://localhost:3000
EXPO_PUBLIC_SOLANA_CLUSTER=mainnet-beta
# Set to "true" once you have a dev client build that ships the MWA native
# module. Default is off so the app runs in Expo Go against a demo wallet.
EXPO_PUBLIC_WALLET_ENABLED=false
